<Window x:Class="Snake.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Snake"
        mc:Ignorable="d"
        Title="Snake" Height="500" Width="800"
        Background="{StaticResource BackgrounColor}"
        Foreground="{StaticResource TextColor}"
        FontFamily="{StaticResource MainFont}"
        WindowStartupLocation="CenterScreen"
        Icon="Assets/icon.ico"
        MinHeight="300"
        MinWidth="300"
        KeyDown="Window_KeyDown"
        >
    <Window.Resources>
        <local:ValToLRMarginConverter x:Key="marginConverter"></local:ValToLRMarginConverter>

        <Style x:Key="CustomButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" >
                                    <Setter.Value>
                                        <SolidColorBrush Color="AliceBlue" Opacity="0.8"/>
                                    </Setter.Value>
                                </Setter>

                                <Setter TargetName="ButtonBorder" Property="BorderBrush" >
                                    <Setter.Value>
                                        <SolidColorBrush Color="DeepSkyBlue" Opacity="0.7"/>
                                    </Setter.Value>
                                </Setter>

                                <Setter TargetName="ButtonBorder" Property="BorderThickness" >
                                    <Setter.Value>
                                        <Thickness Right="1" Top="1" Left="1" Bottom="1"/>
                                    </Setter.Value>

                                </Setter>

                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="TextBlockScoreTab" TargetType="TextBlock">
            <Setter Property="Background" Value="Red"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="TextAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>

    </Window.Resources>

    <Viewbox>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>

            <Button x:Name="ButtonSettings" Style="{StaticResource CustomButtonStyle}" BorderBrush="Transparent" Grid.Row="0" HorizontalAlignment="Left" Margin="0,10,0,0" VerticalAlignment="Top" Click="ButtonSettings_Click" Height="40" Width="40">
                <Button.Background>
                    <SolidColorBrush Color="Transparent"/>
                </Button.Background>

                <Image Source="/assets/SettingsIcon.png"></Image>

            </Button>

            <TextBlock x:Name="ScoreText"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Text="SCORE 0"
                   FontSize="22"
                   Margin="10"
                   />

            <Border x:Name="GridBorder"
                Grid.Row="1"
                BorderBrush="{StaticResource GridLineColor}"
                BorderThickness="1.5"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                RenderOptions.EdgeMode="Aliased"
                Margin="{Binding ElementName=ButtonSettings, Path=ActualWidth, Converter={StaticResource marginConverter}}"
                >

                <UniformGrid x:Name="GameGrid"
                         Width="400"
                         Height="400"
                         Background="{StaticResource GridBackgroundColor}"
                         SnapsToDevicePixels="True"
                         RenderOptions.BitmapScalingMode="HighQuality"
                         />
            </Border>


            <Border x:Name="OverlayPressToStart"
                 Grid.Row="1"
                 Background="Transparent"
                 Width="{Binding Path=ActualWidth, ElementName=GridBorder}"
                 Height="{Binding Path=ActualHeight, ElementName=GridBorder}"
                 Margin="{Binding Path=Margin, ElementName=GridBorder}"
                 RenderOptions.EdgeMode="Aliased"
                    Visibility="Visible">

                <Grid Grid.Row="1" Grid.Column="0" Visibility="Visible">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"></RowDefinition>
                        <RowDefinition Height="*"></RowDefinition>
                        
                    </Grid.RowDefinitions>
                    
                    <TextBlock x:Name="PressToStartText" Visibility="Visible"
                        FontWeight="Bold"
                        Text="PRESS ANY KEY TO START"
                        FontSize="26"
                        TextWrapping="Wrap" 
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Grid.Row="0"/>  
                </Grid>
            </Border>

            <Border x:Name="OverlayScore"
                    Grid.Row="1"
                    Background="{StaticResource OverlayColor}"
                    Width="{Binding Path=ActualWidth, ElementName=GridBorder}"
                    Height="{Binding Path=ActualHeight, ElementName=GridBorder}"
                    Margin="{Binding Path=Margin, ElementName=GridBorder}"
                    RenderOptions.EdgeMode="Aliased">

                <Grid Grid.Row="1" Grid.Column="0" Visibility="Visible">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"></RowDefinition>
                        <RowDefinition Height="auto"></RowDefinition>
                        <RowDefinition Height="*"></RowDefinition>
                    </Grid.RowDefinitions>

                    <!-- row 0 -->
                    <Grid Grid.Row="0" VerticalAlignment="Bottom">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="1" Orientation="Horizontal" >
                            <TextBlock Text="SCORE: " 
                                   FontSize="26" 
                                   />

                            <TextBlock x:Name="ScoreValue"
                                    Text="00"
                                    FontSize="26" 
                                    />
                        </StackPanel>
                    </Grid>

                    <!-- row 1 -->
                    <Grid Grid.Row="1"
                        VerticalAlignment="Stretch"
                        HorizontalAlignment="Stretch"
                        FlowDirection="LeftToRight"
                        Margin="5">

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>

                        <TextBlock 
                            x:Name="NicknameTextBlock"
                        Text="NICKNAME: "
                        FontSize="18"
                        HorizontalAlignment="Right"
                        Grid.Column="0"
                        />

                        <TextBox x:Name="NicknameTextBox"
                        HorizontalAlignment="Left"   
                        Margin="5,0,0,0"
                        Width="100"
                        Grid.Column="1"
                        />
                    </Grid>

                    <!-- row 2 -->
                    <Grid Grid.Row="2" Margin="0,10,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1*"/>
                            <ColumnDefinition Width="1.5*"/>
                            <ColumnDefinition Width="1*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel VerticalAlignment="Stretch"
                            HorizontalAlignment="Stretch"
                            Margin="0"
                            Grid.Column="1">
                            <StackPanel.Resources>
                                <Style x:Key="{x:Type TextBlock}" TargetType="TextBlock" BasedOn="{StaticResource TextBlockScoreTab}"/>
                            </StackPanel.Resources>

                            <!-- TODO: utworzyć proper tabelę -->
                            <TextBlock>"Data1"</TextBlock>
                            <TextBlock>"Data2"</TextBlock>
                            <TextBlock>"Data3"</TextBlock>
                        </StackPanel>
                    </Grid>
                </Grid>
            </Border>

        </Grid>
    </Viewbox>
</Window>
